		
	public void SerializeBooks(Stream xstream)
		{
		XDocument xdoc = new XDocument();
		XElement root = new XElement("<#=code.Escape(entitySet)#>");
		xdoc.Add(root);
		{
<#
			
foreach(var property in entitySet.ElementType.Properties)
{
System.Diagnostics.Debugger.Break();	
	code.SpaceBefore(property.Name);
}
#>		
		}
		xdoc.Save(xstream);
		}
